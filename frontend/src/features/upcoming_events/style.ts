import { card_medium } from "../../assets/styles/cards";

export const cardStyle = { ...card_medium, ...{ display: 'flex' } }

export const parentBoxStyle = { display: 'flex', flexDirection: 'column', margin: 'auto' }

export const minMaxBoxStyle = { display: 'flex', flexDirection: 'row' }